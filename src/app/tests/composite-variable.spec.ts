import { CompositeVariable } from '../classes/composite-variable';

describe('CompositeVariable', () => {
  it('should create an instance', () => {
    expect(new CompositeVariable()).toBeTruthy();
  });
});
